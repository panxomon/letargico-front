export const Parrafo = (props) => {
    return (
        <p>{props.Texto}</p>
    )
}

export const TextoPlano = (props) => {
    return (
        <p>{props.texto}</p>
    )
}

export default TextoPlano